This document has no frontmatter and no heading of any kind, just a plain
paragraph of body text describing some research findings without any clear
title to extract, which should force the parser down the LLM fallback path
and ultimately fail because no title can be determined from the content.
